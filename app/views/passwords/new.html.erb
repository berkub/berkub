<%= simple_form_for :password, url: passwords_path, html: { data: { validate: "parsley" } } do |f| %>
  <h1><%= t("passwords.new.title").html_safe %></h1>

  <%= f.input :email %>

  <div class="actions">
    <%= f.button :submit, t("helpers.submit.password.create") %>
  </div>
<% end %>